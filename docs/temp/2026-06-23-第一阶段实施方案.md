# Ayes 第一阶段实施方案

- [x] 1. 明确第一阶段技术栈与运行形态
  - 目标：确定 Ayes v1 的语言、运行方式、进程监控方案、OCR 方案、向量检索方案、存储方案、企业微信推送方案。
  - 产出：一份可执行的技术选型结论，回写到正式开发文档或新增正式模块文档。
  - 结果：已新增正式文档 `08-技术选型与运行形态.md`，确定第一阶段采用 Python、本地常驻服务、PyObjC + Quartz/AppKit、跨平台 OCR Provider 抽象、SQLite、企业微信 webhook。

- [x] 2. 验证指定进程下窗口发现能力
  - 目标：验证当前系统环境下，是否可以稳定枚举指定进程关联窗口，并拿到窗口身份、标题、层级、最小化/隐藏等状态。
  - 产出：窗口发现能力结论、限制说明、候选实现路径。
  - 结果：已安装 `PyObjC 11.1` 并完成 macOS 初步验证，可拿到 PID、owner、window number、layer、bounds 和部分 onscreen 状态；但窗口标题常为空，且会混入辅助窗口，因此后续必须加入窗口过滤与业务窗口判定逻辑。

- [x] 3. 验证窗口采集可行性
  - 目标：验证前台窗口、后台窗口、被遮挡窗口、最小化窗口的像素采集能力边界。
  - 产出：可采集状态矩阵，以及“状态可观测”和“像素可采集”的差异说明。
  - 结果：已新增 `scripts/verify_window_capture_matrix.py` 和 `src/ayes/targets/capture_matrix.py`，基于窗口候选的可观测状态生成窗口采集可行性矩阵；当前结论正式回写到采集约束与技术选型文档，明确 `recommended / best_effort / metadata_only` 三档可采集预期，以及“元数据可见不等于像素可采集”的约束。

- [x] 4. 确定采样与变化检测链路
  - 目标：确定采样频率、帧差分方式、ROI 检测方式、去重策略。
  - 产出：变化检测最小链路设计。
  - 结果：已新增 `src/ayes/capture/ticker.py`、`src/ayes/capture/models.py`、`src/ayes/capture/screen.py`、`src/ayes/detect/diff.py` 和对应测试，完成第一版 `screenshot tick + byte diff detector` 最小链路，为后续 OCR 和 ROI 检测提供统一上游输入。

- [x] 5. 确定 OCR 处理链路
  - 目标：确定 OCR 引擎、文本清洗规则、文本块切分规则、与窗口/区域绑定的结构格式。
  - 产出：OCR 输入输出约定。
  - 结果：已新增 `src/ayes/ocr/` 模块，落地统一 OCR 输入输出结构、`VisionOCRProvider`、`RapidOCRProvider` 和 `OCRService`；当前本机已验证 `Vision OCR` 可对主屏截图识别出文本，作为 MVP 首条真实可跑 OCR 链路。

- [ ] 6. 确定轻量视觉标签策略
  - 目标：明确第一阶段标签集合、标签触发规则、优先级映射。
  - 产出：`dialog`、`error_like`、`status_change` 等标签的初版判定方案。

- [ ] 7. 确定语义匹配方案
  - 目标：明确 embedding 模型、向量库存储方式、事件与 watch intent 的匹配流程。
  - 产出：语义匹配最小实现设计。

- [x] 7.1 完成 OCR Provider 接口设计
  - 目标：定义跨平台 OCR 抽象接口，确保后续 skill 不绑定单一平台。
  - 产出：OCR Provider 输入输出接口、错误模型、能力标识和候选 Provider 优先级结论。
  - 结果：已新增正式文档 `09-OCRProvider接口设计.md`，当前优先实现 PaddleOCR Provider，保留 RapidOCR 和 Tesseract 替换位。

- [x] 8. 完成 Watch Spec 初版定义
  - 目标：把用户自然语言监控目标转成统一可执行配置结构。
  - 产出：可用于实现的 `watch spec` 正式定义。
  - 补充要求：必须支持 `triggered` 和 `observe` 两种模式，并支持可选 `refresh_click` 受控动作。
  - 结果：已新增正式文档 `10-WatchSpec配置契约.md`，包含任务模式、采样/OCR 频率、短期/长期记忆、webhook 和受控刷新点击策略。

- [x] 9. 完成事件模型初版定义
  - 目标：明确事件字段、证据引用、优先级、可观测状态、语义匹配结果。
  - 产出：正式事件 JSON 结构。
  - 结果：已新增正式文档 `11-事件模型契约.md`，定义基础事件结构、事件类型、事件来源、可观测性、证据引用、动作和告警审计事件。

- [x] 10. 确定短时时间线存储设计
  - 目标：明确最近 5-15 分钟事件如何存、如何查、如何清理。
  - 产出：时间线存储结构与清理策略。
  - 补充要求：短期详细记忆默认最多保留 15 分钟。
  - 结果：已新增正式文档 `12-记忆存储设计.md`，明确短期详细记忆默认和最大保留 15 分钟，基于事件和证据引用查询。

- [x] 10.1 确定长期轻量记忆设计
  - 目标：明确长期监控时保留哪些低密度摘要事件、保留多久、如何与短期详细记忆衔接。
  - 产出：长期轻量时间线结构与过期策略。
  - 结果：已新增正式文档 `12-记忆存储设计.md`，明确长期轻量记忆默认保留 24 小时，每 5 分钟生成摘要，保存高优先级事件、告警、状态变化和阶段摘要。

- [x] 11. 确定问答检索路径
  - 目标：定义“最近几分钟发生了什么”“为什么变成现在这样”的检索和摘要流程。
  - 产出：问答链路设计。
  - 结果：已新增正式文档 `13-问答检索路径.md`，明确采用关键词 + 向量混合检索，短期详细记忆负责 15 分钟内细节问答，长期轻量记忆负责 24-72 小时内的持久化回顾，并支持服务或 skill 重启后接续；用户指定时间范围时，检索必须严格限制在该范围内。

- [x] 12. 确定企业微信 webhook 告警策略
  - 目标：明确触发条件、去重、冷却、消息模板和失败重试策略。
  - 产出：告警策略设计。
  - 结果：已新增正式文档 `14-企业微信Webhook告警策略.md`，明确 triggered 模式默认启用、observe 模式默认不主动告警，定义触发条件、去重、冷却、消息模板、失败处理和告警审计事件。

- [x] 12.1 设计受控刷新点击能力
  - 目标：明确用户如何选择点击点、配置频率、暂停点击，以及点击动作如何进入时间线。
  - 产出：刷新点击配置结构、执行边界和审计事件模型。
  - 结果：已新增正式文档 `15-受控刷新点击策略.md`，明确用户显式选点、窗口/屏幕坐标空间、频控、暂停恢复、安全边界，以及 `refresh_click` / `refresh_click_skipped` / `refresh_click_paused` / `refresh_click_resumed` 审计事件。

- [x] 13. 规划 v1 项目目录结构
  - 目标：在正式实现前明确代码目录、模块边界、配置位置、日志位置、测试目录。
  - 产出：v1 代码结构草案。
  - 结果：已新增正式文档 `16-v1项目目录结构.md`，明确 `src/ayes/` 模块边界、`tests/` 对应结构、`data/` 持久化目录、`runtime/` 运行目录及顶层配置文件布局。

- [x] 13.1 设计监控目标选择交互
  - 目标：明确整个屏幕和窗口候选如何以预览画面 + 进程名 + 标题的形式罗列，支持用户点击选择绑定。
  - 产出：目标选择交互说明与数据结构。
  - 结果：已新增正式文档 `17-监控目标选择交互.md`，明确屏幕/窗口候选预览、业务窗口排序、可观测状态标记、绑定流程，以及与刷新点击和隐式上下文问答的关系。

- [x] 14. 拆解第一批开发任务
  - 目标：将 v1 拆成可执行的小任务，供后续逐项开发。
  - 产出：开发任务清单或正式实施计划文档。
  - 结果：第一批开发任务收口如下，后续实现按顺序推进并逐项验证：
    - 14.1 初始化 Python 工程骨架：补齐 `pyproject.toml`、`src/ayes/`、`tests/`、`data/`、`runtime/` 和基础包结构。
    - 14.2 落地 `watch spec` 配置模型：实现 `triggered` / `observe`、目标配置、采样配置、记忆配置、告警配置、刷新点击配置的结构化校验。
    - 14.3 落地事件模型：实现统一事件数据结构、可观测性结构、`watch_match` 结构和基础事件工厂。
    - 14.4 落地监控目标数据结构：实现屏幕/窗口候选、可观测状态、绑定目标数据结构。
    - 14.5 落地 macOS 窗口发现最小实现：实现窗口枚举、基础过滤、业务候选转换，并保留无 `PyObjC` 时的受控降级。
    - 14.6 落地单元测试最小集合：优先覆盖配置契约解析、事件工厂输出、窗口发现过滤逻辑。
    - 14.7 补齐 CLI / 服务入口骨架：为后续监控任务编排、HTTP API 和 skill 接入预留统一启动点。
    - 14.8 再推进采样、OCR、差分检测和记忆持久化主链路。

- [x] 15. 完成文档回写检查
  - 目标：确认上述结论已同步到正式开发文档，不把临时结论只留在 `docs/temp`。
  - 产出：正式文档更新记录。
  - 结果：已回写正式文档 `03`、`04`、`08`、`09`、`12`、`13`、`16`，并新增 `18-Web工作台与人类测试界面.md`、`19-日志、事件与记忆可视化.md`；同时在 `00-项目规则.md` 中补充“无界面不算真正可用”“必须提供日志/事件/记忆可视化”“前端保持简洁无卡片化”“保留 agent 接入服务边界”等强约束。

- [ ] 17. 推进 Web 工作台、日志可视化与 Agent 接入边界落地
  - 目标：把当前 CLI MVP 扩展为带本地 Web 工作台、可查看记忆/近期事件/日志、并保留 API 服务边界的下一阶段可测试版本。
  - 产出：前端工作台、日志能力、HTTP/API 基础接口、可人工测试的工作台链路。
  - 当前拆分：
    - [x] 17.1 提供本地 Web 工作台基础页面与 HTTP 服务入口
    - [x] 17.2 提供近期事件、截图预览、短期问答、日志查看基础界面
    - [x] 17.3 提供长期摘要与任务 id 输入入口
    - [x] 17.4 让任务 id / 最近 N 分钟真正作用于事件、问答、日志读取
    - [x] 17.5 增加日志分类过滤与更清晰的结构化问答展示
    - [x] 17.6 收口更正式的 Agent 调用契约与对应接口模型
    - [x] 17.7 提供屏幕候选 + 窗口候选的统一目标选择接口
    - [x] 17.8 提供最小 watch spec 配置表单并支持直接装载任务
    - [x] 17.9 提供后台持续监控 start/stop/status 基础接口
    - [x] 17.10 前端接入持续监控控制与运行状态可视化
    - [x] 17.11 将 refresh_click 接入监控循环并输出 action 审计日志
    - [x] 17.12 增加动作计数、动作轨迹与最近执行状态可视化
    - [x] 17.13 增加命中/告警时间线面板与状态计数接口
    - [ ] 17.14 补齐日志/事件/记忆联合查看体验，确保工作台可直接排查“为什么没有触发/为什么没有告警”
      - [x] 17.14.1 固化最小 HTTP 闭环 smoke，确保 status/timeline/snippets/logs/ask 可重复验证
      - [x] 17.14.2 为事件/片段/动作/命中/告警/长期摘要/日志面板补齐查询上下文显示与空结果解释
      - [x] 17.14.3 为短期记忆区补齐“最近记忆条目”列表，支持按当前任务、时间范围和关键词核验近期保留事件
      - [x] 17.14.4 为 OCR 事件补齐轻量质量摘要，便于人工判断本次识别是否稀疏或不可靠
      - [x] 17.14.5 为视觉增强补齐 `vision_triggered` / `vision_skipped` 审计事件，显式记录触发原因与阻断原因
      - [x] 17.14.6 新增独立视觉轨迹面板，集中查看 vision 相关事件
      - [x] 17.14.7 为 vision 结果补齐细节行保留与展示，减少多行视觉信息被压扁成单句摘要
      - [x] 17.14.8 为问答结果补齐 `structured_vision_matches`，集中展示命中的 vision 摘要与细节行
    - [x] 17.15 补齐最小可用告警发送链路与告警审计事件
    - [x] 17.16 补齐 OCR 片段辅助查看，降低 triggered 关键词配置门槛
    - [x] 17.17 收敛工作台布局与状态呈现，满足“简洁高密度、少卡片、可直接核验”的正式规则

- [ ] 18. 推进可人工核验的最小核心链路 MVP 收口
  - 目标：完成“选目标 -> 配置任务 -> 启动或执行监控 -> 查看近期事件/短期记忆/长期摘要/日志 -> 发起提问”的可人工核验闭环。
  - 产出：真实可操作的本地 Web 工作台 MVP、对应 API 能力、最小测试与人工验证记录。
  - 当前拆分：
    - [x] 18.1 统一当前任务、运行状态、最近执行、最近命中、最近告警的工作台主视图
    - [x] 18.2 补齐问答结果中的时间范围、证据引用、记忆层展示
    - [ ] 18.3 补齐近期事件、动作、命中、告警、日志的任务与时间范围联动
      - [x] 18.3.1 当前已补齐 timeline/logs/ask/snippets 的任务与 minutes 范围读取，并增加最小闭环 smoke 校验
      - [x] 18.3.2 当前已补齐主要列表面板的 `task_id / minutes / source|category / count` 显示，空结果时可直接解释当前过滤范围
    - [x] 18.4 完成最小人工验证步骤文档回写，确保用户可直接按界面操作测试
    - [x] 18.5 运行关键测试并清理明显交互缺口，达到“无需命令行即可主要使用”的标准

- [ ] 16. 开发完成后重命名本文件
  - 目标：当第一阶段实施、开发、验证完成后，将本文件改名为 `2026-06-23-已完成-第一阶段实施方案.md`。
  - 产出：完成态临时实施文档。
